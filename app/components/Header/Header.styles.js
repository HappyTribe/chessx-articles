import styled from 'styled-components'

const Wrapper = styled.header`
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    height: 4rem;
    background-color: #1e1e1e;
    padding: 0.5rem;
`

const HeaderTitle = styled.h1`
    color: #ececec;
`

export { Wrapper, HeaderTitle }
